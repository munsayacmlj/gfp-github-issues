This project is using Laravel version 12.x and PHP 8.4

Copy .env.example to .env.local

Ensure that you have APP_KEY

Provide your GITHUB_PERSONAL_TOKEN in .env.local

Run the commands\
`npm install && npm run build`\
`composer run dev`
